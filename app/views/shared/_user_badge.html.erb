<section id="user-badge">
	<div class="row">
		<div class="span2">
			<% if user.class == User%>
			<div class="image1">				
				<%= link_to image_tag(user.avatar.url(:small)), user, class: "image"%>
			</div>
			<div class="nom">
				<%= link_to user.name, user, class: "nom"%>
			</div>
			<div class="contexte">
					<%= h Rinku.auto_link(user.content).gsub(/\n/, '<br/>').html_safe%>
					<%= h Rinku.auto_link(user.url).gsub(/\n/, '<br/>').html_safe%>
			<div>
							 
			<% else%>  
			<div class="image1">
				<%= link_to image_tag(user.avatar.url(:medium)), "http://#{user.url}", class: "image"%>
			</div>
			<div class="nom">
				<%= link_to user.name, "http://#{user.url}", class: "nom"%>
			</div>
			<div class="contexte">
					<%= h Rinku.auto_link(user.url).gsub(/\n/, '<br/>').html_safe%>
			<div>
			<% end%>				
		</div>
	</div>
</section>