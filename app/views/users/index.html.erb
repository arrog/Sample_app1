<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
		<% @users.each do |l|%>
		<div>
			<%= l.email%>
			<%= l.name %>
		</div>
		<% end%>
</ul>

<%= will_paginate %>